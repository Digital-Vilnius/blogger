{% extends 'user/base.html.twig' %}
{% set title = 'comments' | trans %}
{% set colspan = 6 %}
{% set header_active = blog.name %}
{% set subheader_active = 'posts' %}

{% block main %}
    <table class="table">
        <thead>
        <tr>
            {% include 'shared/includes/sortable-column.html.twig' with {title: 'username' | trans, column: 'username', class: 'left' } %}
            {% include 'shared/includes/sortable-column.html.twig' with {title: 'updated' | trans, column: 'updated', class: 'medium-column center' } %}
            {% include 'shared/includes/sortable-column.html.twig' with {title: 'created' | trans, column: 'created', class: 'medium-column center' } %}
            <th class="center medium-column">{{ 'actions' | trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for comment in comments %}
            <tr>
                <td class="ellipsis"><div>{{ comment.username }}</div></td>
                <td class="center">{{ comment.updated | dateTimeFormat }}</td>
                <td class="center">{{ comment.created | dateTimeFormat }}</td>
                <td class="center">
                    <div class="dropdown">
                        <div class="dropdown-toggle">
                            <i class="material-icons">more_horiz</i>
                        </div>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('blog category delete', {blogId: blog.id, id: comment.id}) }}">{{ 'delete' | trans }}</a>
                        </div>
                    </div>
                </td>
            </tr>
        {% endfor %}
        {% if comments | length == 0 %}
            <tr>
                <td class="no-data" colspan="{{ colspan }}">{{ 'no_data' | trans }}</td>
            </tr>
        {% endif %}
        </tbody>
        <tfoot>
        <tr>
            <td colspan="{{ colspan }}">
                <div class="paging-container">
                    <div class="count">{{ 'count' | trans }} : <b>{{ commentsCount }}</b></div>
                    {% include 'shared/includes/paging.html.twig' %}
                </div>
            </td>
        </tr>
        </tfoot>
    </table>
{% endblock %}