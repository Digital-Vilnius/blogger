{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.blog) }}
<button class="button primary" type="submit">{{ 'save' | trans }}</button>
{{ form_end(form) }}