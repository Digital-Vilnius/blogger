{% extends 'admin/base.html.twig' %}
{% set title = 'blogs' | trans %}
{% set header_active = 'blogs' %}
{% set colspan = 7 %}

{% block additional_header %}
    <a class="button primary" href="{{ path('admin blog add') }}">{{ 'add_blog' | trans }}</a>
{% endblock %}

{% block main %}
    <table class="table">
        <thead>
        <tr>
            <th class="sortable center small">{{ 'id' | trans }}</th>
            <th class="sortable">{{ 'name' | trans }}</th>
            <th class="sortable">{{ 'user' | trans }}</th>
            <th class="sortable">{{ 'origin' | trans }}</th>
            <th class="sortable center">{{ 'updated' | trans }}</th>
            <th class="sortable center">{{ 'created' | trans }}</th>
            <th class="center">{{ 'actions' | trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for blog in blogs %}
            <tr>
                <td class="center">{{ blog.id }}</td>
                <td class="ellipsis"><div>{{ blog.name }}</div></td>
                <td class="ellipsis"><div>{{ blog.user.email }}</div></td>
                <td class="ellipsis"><div>{{ blog.origin }}</div></td>
                <td class="center">{{ blog.updated | dateTimeFormat }}</td>
                <td class="center">{{ blog.created | dateTimeFormat }}</td>
                <td class="center">
                    <div class="table-actions">
                        <a title="{{ 'edit' | trans }}" href="{{ path('admin blog edit', {id: blog.id}) }}"><i class="material-icons">create</i></a>
                        <a title="{{ 'delete' | trans }}" href="{{ path('admin', {id: blog.id}) }}"><i class="material-icons">delete</i></a>
                    </div>
                </td>

            </tr>
        {% endfor %}
        {% if blogs | length == 0 %}
            <tr>
                <td class="no-data" colspan="{{ colspan }}">{{ 'no_data' | trans }}</td>
            </tr>
        {% endif %}
        </tbody>
        <tfoot>
        <tr>
            <td colspan="{{ colspan }}">
                <div class="paging-container">
                    <div class="count">{{ 'count' | trans }} : <b>{{ blogsCount }}</b></div>
                    {% include 'shared/includes/paging.html.twig' %}
                </div>
            </td>
        </tr>
        </tfoot>
    </table>
{% endblock %}